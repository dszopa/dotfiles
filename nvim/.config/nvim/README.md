Jotting down some stuff in here that I'm bound to forget when configuring neovim:

1. When using `require` in `~/.config/nvim/init.lua`, you must have a `lua/` folder next to the `init.lua` file where `require` will source from (if not global).